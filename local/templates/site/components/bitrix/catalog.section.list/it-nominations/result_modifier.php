<?
if (!defined("B_PROLOG_INCLUDED") || B_PROLOG_INCLUDED!==true) die();

/*
$sections=[];
foreach($arResult['SECTIONS'] as $section){
   $sections[$section['SORT']]=$section;
}

ksort($sections);
$arResult['SECTIONS']=$sections;
*/